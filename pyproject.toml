[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "colocon"
description = "Run colcon against repositories laid out as git worktrees."
readme = "README.md"
requires-python = ">=3.9"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{name = "Ricardo González", email = "correoricky@gmail.com"}]
keywords = ["colcon", "ros", "ros2", "git-worktree", "build"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Topic :: Software Development :: Build Tools",
]
dynamic = ["version"]
dependencies = ["pyyaml>=5.1"]

[project.optional-dependencies]
dev = ["pytest>=7", "ruff>=0.6", "mypy>=1.11", "types-PyYAML"]

[project.urls]
Homepage = "https://github.com/richiware/colocon"
Source = "https://github.com/richiware/colocon"
Issues = "https://github.com/richiware/colocon/issues"

[project.scripts]
colocon = "colocon.core:main"

[tool.setuptools]
packages = ["colocon"]

[tool.setuptools.dynamic]
version = {attr = "colocon.__version__"}

[tool.ruff]
line-length = 120
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.9"
warn_unused_configs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
